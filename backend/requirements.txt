torch>=2.1.0
torchvision>=0.16.0
scikit-learn>=1.3.0
numpy>=1.24.0
Pillow>=10.0.0
matplotlib>=3.7.0
tqdm>=4.66.0
pandas>=2.0.0
requests>=2.31.0

# Optional — only needed for the real GenAI conversational assistant
# (src/genai_assistant.py). Everything else in this project works without
# it and without an internet connection.
# anthropic>=0.40.0

# Khedut Mitr — Gemini API backend
google-genai>=2.3.0
python-dotenv>=1.0.0
Flask>=3.0.0
flask-cors>=4.0.0
